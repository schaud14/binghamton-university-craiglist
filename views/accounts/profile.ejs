<% layout('layout') -%>

<div class="container">
  <div class="row">
    <div class="col-sm-6 col-md-4">
      <img class="img-circle img-responsive img-center" src="<%= user.profile.picture %>" alt="">

      <br />
      <p class='textcolor'>Name: <%= user.profile.name %></p>
      <p class='textcolor'>Email: <%= user.email %></p>

      <% if (user.address === "" || typeof user.address === 'undefined')  { %>
        <p class='textcolor'>Address: <span style="color: #c0392b; font-weight: bold">You have no address</span></p>

      <% } else { %>

      <p class='textcolor'>Address: <%= user.address %></p>

      <% } %>

      <a href="/edit-profile" class="btn btn-primary btn-lg">Edit Profile</a>
    </div>

    <div class="col-md-6">
      <h2 class="text-center textcolor">History</h2>

      <table class="table table-striped sticky-header">
        <thead>
        <tr>
          <th class='textcolor'>Item's Name</th>
          <th class='textcolor'>Paid: </th>
        </tr>
        <tbody>
          <% for(var i = 0; i < user.history.length; i++) {%>
            <tr>
              <td class=''><%= user.history[i].item.name %></td>
              <td class=''>$<%= user.history[i].paid %></td>
            </tr>
          <% } %>
        </tbody>
        </thead>
      </table>

    </div>
  </div>
</div>
